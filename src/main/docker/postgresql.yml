version: '2'
services:
  portalcamlar-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/portalCamlar/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=portalCamlar
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
